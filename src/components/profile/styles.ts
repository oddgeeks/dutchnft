import styled from 'styled-components';

export const ProfileWrapper = styled.div.attrs({
  className: 'flex gap-gap-6 items-start',
})``;

export const ProfileInner = styled.div.attrs({
  className: 'flex flex-col gap-4 w-2/3',
})``;

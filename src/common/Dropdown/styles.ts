import styled from 'styled-components';

// types

// components
// --- Dropdown(default)
export const DropdownWrapper = styled.div.attrs({
  className:
    'inline-flex items-center space-x-1 w-full h-9.5 border border-black/10 text-sm rounded text-black/70 hover:border-black hover:text-black/90 disabled:border-black/30 disabled:opacity-50 transition',
})``;

import React from 'react';

// components
import { AppLayout, CreateHome } from '@/components';

export default function Create() {
  return (
    <AppLayout>
      <CreateHome />
    </AppLayout>
  );
}

import React from 'react';

// components
import { AppLayout, CreateCollectionHome } from '@/components';

export default function CreateCollection() {
  return (
    <AppLayout>
      <CreateCollectionHome />
    </AppLayout>
  );
}
